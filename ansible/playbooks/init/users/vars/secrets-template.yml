# TODO: secrets.yml will be replaced with Hashicorp Vault calls

ansible_password: "{{ 'PASSWORD' | password_hash('sha512', 'SALT') }}"
personal_password: "{{ 'PASSWORD' | password_hash('sha512', 'SALT') }}"
