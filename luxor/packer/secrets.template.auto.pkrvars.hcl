# proxmox_api_token_id = "packer@pve!packer" # user@realm!token
# proxmox_api_token_secret = "TOKEN_SECRET"

# generate hash with 
#   openssl passwd -1 -salt SOME_SALT ROOT_PASSWORD

# root_password_hash = "$1$SOME_SALT$XXX"